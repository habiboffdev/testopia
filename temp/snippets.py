# self.last_question = self.not_answered[-1]
# get = self.answers.get(self.last_question, False)
# while not get and len(self.not_answered):
#     self.not_answered.pop(self.last_question)
#     self.last_question = self.not_answered[-1]
#     get = self.answers.get(self.last_question, False)
# if get:
#     return -1

# ---